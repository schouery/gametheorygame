%p.title 
  Result for 
  = @card.game.name
  
%p
  Your payoff:
  = @card.payoff
  
%p
  Your Player Number:
  = @card.player_number
  
%p
  You choose the strategy
  = @card.strategy.label

.alternating_table
  %table
    %tr.odd
      %th Player
      %th Strategy
      %th Payoff
    -@card.game_result.cards.sorted_by_player_number.each_with_index do |card, i|
      %tr{:class => (i.odd? ? "odd" : "even")}
        %td=i+1
        %td=card.strategy.label
        %td=card.payoff